
Clear path. Add https://github.com/vandermeerlab/vandermeerlab/code-matlab/shared 

[csc_tsd, orientation, samplingrate, dt] = GetOrientationValues([]);
[orientationtouse] = downsampleOrientationValues(orientation, 10);
[AHVtsd] = GetAHV_values(orientationtouse);

newrange = AHVtsd.tvec - AHVtsd.tvec(1);
AHVtsdnew = tsd(newrange, AHVtsd.data);

[manually add zero before any TT numbers in the single digits -- make a function for this]
S= LoadSpikes([]);

Q = MakeQfromS([],S);

cfg_in.dt = .05;
F = tsd(Q.tvec, Q.data.*1/cfg_in.dt);


[AHV_tsd, S, tc_out] = AHV_tuning([]);
plot(tc_out.usr.binCenters, tc_out.tc, 'LineWidth', 5)
